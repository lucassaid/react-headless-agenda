import { Canvas, Meta, Source } from '@storybook/blocks';
import * as ColumnsStories from './Columns.stories';

<Meta of={ColumnsStories} />

# Columns

Use `<Columns>` to render a markup for each day

<Canvas of={ColumnsStories.ColumnsHeader} />
 
<Source
  code={`
<div className="flex max-w-lg">
  <Columns>
    {({ date, key }) => (
      <div key={key} className="flex-1 center">
        {format(date, 'ccc d')}
      </div>
    )}
  </Columns>
</div>
  `}
  language="tsx"
/>
